# Bind-mount target for the importer (docs/backend-spec.md §4.1).
# Drop export JSON files here; the container sees them read-only at
# /data/import/. The files themselves are gitignored -- they are personal data.
